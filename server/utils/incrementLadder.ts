/**
 * Re-exports from shared/incrementLadder.ts — the single source of truth.
 * Do not add logic here; edit shared/incrementLadder.ts instead.
 */
export {
  getSpeciesIncrement,
  calculateNextBidPerM3,
  isValidBidIncrement,
  getSpeciesIncrements,
  calculateProjectedTotal,
  SPECIES_INCREMENT_MAP,
  DEFAULT_INCREMENT_PER_M3,
  type SpeciesIncrement,
} from "@shared/incrementLadder";
